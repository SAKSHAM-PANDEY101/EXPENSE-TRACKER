# Copy this file to .env and replace the placeholder with your MongoDB connection string

# For a local MongoDB server (default used if MONGO_URI is not set):
# MONGO_URI=mongodb://127.0.0.1:27017/expense-tracker

# For MongoDB Atlas it looks like:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd123.mongodb.net/expense-tracker?retryWrites=true&w=majority

# Optional - PORT the server will listen on (defaults to 5000)
# PORT=5000
